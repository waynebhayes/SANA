# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test:
    machine:
      image: ubuntu-2004:202010-01
      
    steps:
      - checkout
      
      # - run:
      #     name: "first stack check"
      #     command: "ulimit -s"

      # - run:
      #     name: "set unlimited stack space"
      #     command: "ulimit -s unlimited;ulimit -s"
          
      # - run:
      #     name: "second stack check"
      #     command: "ulimit -s"
      
      # - run:
      #     name: "check g++ version"
      #     command: "g++ -v"
      # - run: 
      #     name: "checkout libwayne"
      #     command: "git submodule init libwayne && git submodule update libwayne && cd libwayne && git checkout master && git pull"
      - run:
          name: "run regression test"
          command: "./regression-test-all.sh -make"


# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  test-workflow:
    jobs:
      - test
