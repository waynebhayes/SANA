#ifndef REVERSEPERMUTATION_HPP
#define REVERSEPERMUTATION_HPP

#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <cmath>

void extractCanoonicals(uint k);
std::string reverseperm(std::string original);
void convertData(uint k);

#endif